# Defaults for every post in posts/ (Quarto merges this into each post's
# front matter). It does not apply to root pages such as the home listing or
# the submission form, so giscus comments appear on posts only.

# giscus comments, backed by GitHub Discussions on the site repo.
# The "Comments" category uses the Announcement format, so only the giscus app
# opens a thread per post and readers only reply. repo-id and category-id come
# from the repo's GraphQL API.
comments:
  giscus:
    repo: bdpedigo/nftn
    repo-id: "R_kgDOUnpBGg"
    category: "Comments"
    category-id: "DIC_kwDOUnpBGs4DGQeH"
    # One discussion per post URL path.
    mapping: pathname
    reactions-enabled: true
    input-position: bottom
    theme: light
    language: en
    loading: lazy
